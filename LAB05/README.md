in order to compile: gcc -O main.c -o ./test

********************************************

This C program simulates 2D incompressible fluid flow using the vorticity-stream function formulation. 
It iteratively solves the Navier-Stokes equations using the Gauss-Seidel method to calculate the stream function, 
vorticity, and velocity fields for various flow rates.
